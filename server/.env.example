# Environment Configuration - Show do Melzão

# Security
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-change-this-in-production

# Server
PORT=5001
NODE_ENV=development

# ==============================================
# DATABASE CONFIGURATION
# ==============================================
# The application supports both SQLite (dev) and PostgreSQL (prod)
# It automatically detects which to use based on DATABASE_URL

# OPTION 1: SQLite (for local development)
# Uncomment the line below and comment out DATABASE_URL
DATABASE_PATH=./game.db

# OPTION 2: PostgreSQL (for production on Render)
# Uncomment the line below and comment out DATABASE_PATH
# DATABASE_URL=postgresql://user:password@host:5432/database

# OPTION 3: PostgreSQL with individual components (alternative)
# Uncomment the lines below if you prefer granular configuration
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=melzao_db
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_SSL=false  # Set to 'true' for Render or other cloud providers

# ==============================================
# PRODUCTION SETTINGS (Render)
# ==============================================
# When deploying to Render:
# 1. Render automatically provides DATABASE_URL for PostgreSQL
# 2. Set NODE_ENV=production
# 3. Update JWT_SECRET with a secure random string
# 4. DATABASE_PATH will be ignored if DATABASE_URL is present

# Rate Limiting (opcional - para uso com Redis)
# REDIS_URL=redis://localhost:6379

# CORS Origins (opcional - sobrescrever defaults)
# ALLOWED_ORIGINS=https://zurcleo.github.io,http://localhost:3001
